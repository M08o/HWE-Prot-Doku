\section{ESP32-Kommunikation und experimenteller Aufbau}
Nach der Schaltungserweiterung wird festgelegt, welche Aufgaben die beiden ESP32 übernehmen sollen. Dazu werden zwei Programme vorgesehen: eines für das Lesegerät und eines für den Tag.

\subsection{Aufgabe der beiden ESP32}
Der ESP32 beim Lesegerät soll das digitale Sendesignal für die ASK-Modulation erzeugen und anschließ end die Antwort des Tags empfangen. Das empfangene Byte wird im Serial Monitor ausgegeben. Danach sendet das Lesegerät wieder ein eigenes Byte zurück.

Der ESP32 beim Tag soll starten sobald der Tag versorgt wird. Danach soll er ein erstes Byte an das Lesegerät senden, auf die Antwort warten und anschließ end im Wechselbetrieb weiter kommunizieren.

\subsection{Geplante UART-Kommunikation}
Für die Datenübertragung wird eine UART-Verbindung mit $1200\,Baud$ und 8-N-1 vorgesehen. Die niedrige Baudrate ist für den Versuchsaufbau sinnvoll, weil die Modulations- und Demodulationsschaltungen dadurch mehr Zeit haben, die einzelnen Signalzustände sauber abzubilden.

Das Sendesignal des Lesegeräts wird dabei als ASK-Signal interpretiert. Ist TX logisch High, dann ist das Sendesignal aktiv. Ist TX logisch Low, dann ist das Sendesignal aus. Der Tag empfängt dieses Signal und sendet anschließ end über seine Modulationsstufe zurück.

\subsection{Vorversuch ohne vollständige RFID-Schaltung}
Bevor die ESP32 mit der vollständigen Tag- und Lesegerät-Schaltung verbunden werden, wurde die Kommunikation zuerst ohne die HF- beziehungsweise RFID-Schaltung getestet. Dazu wurden die beiden ESP32 direkt miteinander verbunden. Der TX-Ausgang des einen ESP32 wurde mit dem RX-Eingang des anderen ESP32 verbunden und umgekehrt.

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[transform shape]
  	% Paths, nodes and wires:
  	\draw (3.625, 7.25) to[european resistor] (3.625, 5.25);
  	\node[anchor=east] at (3.35, 6.25){\tiny $220\,\Omega$};
  	\draw (3.625, 5.25) to[empty led] (3.625, 3.75);
  	\draw (5.125, 7.25) to[european resistor] (5.125, 5.25);
  	\node[anchor=east] at (4.85, 6.25){\tiny $220\,\Omega$};
  	\draw (5.125, 5.25) to[empty led] (5.125, 3.75);
  	\node[tground] at (3.625, 3.75){};
  	\node[tground] at (5.125, 3.75){};
  	\node[shape=rectangle, draw, line width=1pt, minimum width=3.965cm, minimum height=2.965cm] at (7.875, 7.5){};
  	\node[shape=rectangle, minimum width=-0.035cm, minimum height=-0.035cm] at (6.875, 6.5){};
  	\node[shape=rectangle, minimum width=2.465cm, minimum height=0.59cm] at (7.125, 8.25){} node[anchor=north west, align=left, text width=2.077cm, inner sep=6pt] at (5.875, 8.562){\tiny G18 $\rightarrow$ TX-LED};
  	\node[shape=rectangle, minimum width=2.215cm, minimum height=0.59cm] at (7, 7.25){} node[anchor=north west, align=left, text width=1.827cm, inner sep=6pt] at (5.875, 7.563){\tiny G19 $\rightarrow$ RX-LED};
  	\draw (5.125, 7.25) -- (5.875, 7.25);
  	\draw (3.625, 7.25) -- (3.625, 8.25) |- (5.875, 8.25);
  	\node[shape=rectangle, minimum width=3.09cm, minimum height=0.715cm] at (7.875, 6.625){} node[anchor=north, align=center, text width=2.702cm, inner sep=6pt] at (7.875, 7){\textcolor{rgb,255:red,4;green,210;blue,250}{ESP-Lesegerät}};
  	\node[shape=rectangle, minimum width=0.84cm, minimum height=0.465cm] at (7.875, 6.25){} node[anchor=north west, align=left, text width=0.452cm, inner sep=6pt] at (7.438, 6.5){\tiny GND};
  	\node[tground] at (7.875, 5.5){};
  	\draw (7.875, 6) -| (7.875, 5.5);
  	\node[shape=rectangle, minimum width=1.715cm, minimum height=0.59cm] at (9, 8.25){} node[anchor=north east, align=right, text width=1.327cm, inner sep=6pt] at (9.875, 8.562){\tiny G17 $\rightarrow$ TX};
  	\node[shape=rectangle, minimum width=1.715cm, minimum height=0.59cm] at (9, 7.25){} node[anchor=north east, align=right, text width=1.327cm, inner sep=6pt] at (9.875, 7.562){\tiny G16 $\rightarrow$ RX};
  	\draw (16.25, 7.188) to[european resistor] (16.25, 5.188);
  	\node[anchor=west] at (16.525, 6.188){\tiny $220\,\Omega$};
  	\draw (16.25, 5.188) to[empty led] (16.25, 3.687);
  	\draw (17.75, 7.188) to[european resistor] (17.75, 5.188);
  	\node[anchor=west] at (18.025, 6.188){\tiny $220\,\Omega$};
  	\draw (17.75, 5.188) to[empty led] (17.75, 3.687);
  	\node[tground] at (16.25, 3.687){};
  	\node[tground] at (17.75, 3.687){};
  	\node[shape=rectangle, draw, line width=1pt, minimum width=3.965cm, minimum height=2.965cm] at (13.125, 7.437){};
  	\node[shape=rectangle, minimum width=-0.035cm, minimum height=-0.035cm] at (12.125, 6.437){};
  	\node[shape=rectangle, minimum width=2.215cm, minimum height=0.59cm] at (14, 7.188){} node[anchor=north east, align=right, text width=1.827cm, inner sep=6pt] at (15.125, 7.5){\tiny G18 $\rightarrow$ TX-LED};
  	\node[shape=rectangle, minimum width=2.09cm, minimum height=0.59cm] at (14.062, 8.188){} node[anchor=north east, align=right, text width=1.702cm, inner sep=6pt] at (15.125, 8.5){\tiny G19 $\rightarrow$ RX-LED};
  	\node[shape=rectangle, minimum width=2.027cm, minimum height=0.715cm] at (13.125, 6.562){} node[anchor=north, align=center, text width=1.639cm, inner sep=6pt] at (13.125, 6.937){\textcolor{rgb,255:red,4;green,210;blue,250}{ESP-Tag}};
  	\node[shape=rectangle, minimum width=0.84cm, minimum height=0.465cm] at (13.125, 6.187){} node[anchor=north west, align=left, text width=0.452cm, inner sep=6pt] at (12.688, 6.437){\tiny GND};
  	\node[tground] at (13.125, 5.438){};
  	\draw (13.125, 5.937) -| (13.125, 5.438);
  	\node[shape=rectangle, minimum width=1.715cm, minimum height=0.59cm] at (12, 7.25){} node[anchor=north west, align=left, text width=1.327cm, inner sep=6pt] at (11.125, 7.562){\tiny G17 $\rightarrow$ TX};
  	\node[shape=rectangle, minimum width=1.715cm, minimum height=0.59cm] at (12, 8.25){} node[anchor=north west, align=left, text width=1.327cm, inner sep=6pt] at (11.125, 8.562){\tiny G16 $\rightarrow$ RX};
  	\draw (17.75, 7.188) |- (15.125, 8.188);
  	\draw (16.25, 7.188) -- (15.125, 7.188);
  	\node[vcc] at (13.125, 8.937){};
  	\node[vcc] at (7.875, 9){};
  	\node[shape=rectangle, minimum width=1.465cm, minimum height=0.465cm] at (7.875, 8.75){} node[anchor=north, align=center, text width=1.077cm, inner sep=6pt] at (7.875, 9){\tiny USB-Vcc\\};
  	\node[shape=rectangle, minimum width=1.465cm, minimum height=0.465cm] at (13.125, 8.687){} node[anchor=north, align=center, text width=1.077cm, inner sep=6pt] at (13.125, 8.937){\tiny USB-Vcc\\};
  	\draw (9.875, 8.25) -- (11.125, 8.25);
  	\draw (9.875, 7.25) -- (11.125, 7.25);
  \end{tikzpicture}
  }
  \caption{Experimenteller Direktaufbau der beiden ESP32 ohne RFID-Schaltung}
  \label{fig:esp32_direktaufbau}
\end{figure}

Der direkte Versuchsaufbau ist in \cref{fig:esp32_direktaufbau} dargestellt. Dieser Vorversuch ist wichtig, weil damit die Software und die UART-Kommunikation unabhängig von der analogen Modulationsstrecke geprüft werden können. Wenn die Daten direkt zwischen den beiden ESP32 korrekt übertragen werden, kann ein späterer Fehler leichter zugeordnet werden.

\subsection{Geplanter Ablauf}
Der Ablauf beginnt damit, dass sich der Tag dem Lesegerät nähert und dadurch versorgt wird. Nach dem Einschalten startet der ESP32 des Tags und sendet sofort ein erstes Byte an das Lesegerät. Das Lesegerät empfängt dieses Byte, gibt den Wert im Serial Monitor aus und sendet anschließ end ein eigenes Byte zurück.

Danach soll ein dauerhafter Wechselbetrieb entstehen. Tag und Lesegerät senden sich jeweils ein Byte und warten anschließ end auf die Antwort der Gegenseite. Zwischen den einzelnen Übertragungen ist ein Abstand von ungefähr $3\,s$ vorgesehen. Dieser Ablauf läuft so lange weiter, bis die Versorgung des Tags wieder wegfällt.

\subsection{Geplante Protokollerweiterungen}
Zusätzlich zum einfachen Wechselbetrieb sind drei Erweiterungen für die ESP32-Kommunikation vorgesehen. Diese Erweiterungen betreffen nicht die analoge Grundschaltung, sondern das Kommunikationsprotokoll zwischen Tag und Lesegerät.

\subsubsection{Identifikationsprüfung mit Timeout}
Als erste Erweiterung soll eine einfache Identifikationsprüfung eingebaut werden. Dadurch soll das Lesegerät erkennen, ob sich ein passender Tag im Feld befindet. Der Tag sendet nach dem Start zuerst das Startbyte \texttt{0xAA}. Das Lesegerät wartet auf dieses Byte und verwirft alle anderen empfangenen Werte. Wird \texttt{0xAA} korrekt erkannt, antwortet das Lesegerät mit \texttt{0xAC} als Bestätigung. Erst danach beginnt der eigentliche Datenaustausch.

Der Timeout-Mechanismus gehört dabei zur Identifikationsprüfung. Sobald das Lesegerät die Bestätigung gesendet hat, wird überwacht, ob weiterhin Bytes vom Tag empfangen werden. Wenn innerhalb von ungefähr $4\,s$ kein Byte ankommt, geht das Lesegerät wieder in den Ausgangszustand zurück und wartet erneut auf das Startbyte \texttt{0xAA}. Dadurch wird erkannt, wenn der Tag wieder aus dem Feld entfernt wurde oder nicht mehr versorgt ist.

\subsubsection{LED-Toggle-Befehl}
Als zweite Erweiterung soll der Tag einen einfachen Steuerbefehl an das Lesegerät senden können. Dafür wird das Byte \texttt{0xF0} verwendet. Der Tag sendet im normalen Betrieb Zählerbytes, ersetzt aber jedes fünfte gesendete Byte durch \texttt{0xF0}. Eine mögliche Folge ist daher:
\[
\texttt{0xC0,\ 0xC1,\ 0xC2,\ 0xC3,\ 0xF0,\ 0xC4,\ ...}
\]

Empfängt das Lesegerät \texttt{0xF0}, wird dieses Byte nicht als normales Datenbyte behandelt, sondern als Befehl. In diesem Fall werden die beiden LED-Ausgänge des Lesegeräts dauerhaft umgeschaltet. Dadurch kann gezeigt werden, dass nicht nur Daten übertragen werden, sondern dass der Tag auch eine gezielte Aktion beim Lesegerät auslösen kann.

\subsubsection{Manchester-Kodierung}
Als dritte Erweiterung ist eine Manchester-Kodierung vorgesehen. Dabei werden die Daten nicht mehr direkt als einfache UART-Bits betrachtet, sondern jedes Datenbit wird durch zwei Signalzustände dargestellt. Ein Datenbit \texttt{1} kann zum Beispiel als \texttt{01} und ein Datenbit \texttt{0} als \texttt{10} codiert werden. Dadurch entsteht bei jedem Bit eine Flanke, was für eine spätere Auswertung des modulierten Signals hilfreich ist.

Da jedes Datenbit auf zwei gesendete Bits erweitert wird, muss für die Umsetzung mit UART die Baudrate verdoppelt werden. Bei einer gewünschten effektiven Datenrate von $1200\,Baud$ würde der ESP32 daher mit $2400\,Baud$ senden. Ein Datenbyte wird beim Senden in zwei UART-Bytes umgewandelt und beim Empfangen wieder zurückdecodiert. Diese Manchester-Kodierung ist als nächster Ausbauschritt vorgesehen und noch nicht Teil des einfachen Grundbetriebs.

\subsection{Hinweise zur Software}
Die Programme sollen im Arduino-Framework für den ESP32 erstellt werden. Die Pinbelegung wird im Code übersichtlich am Anfang angegeben. Zusätzlich soll der Tag-ESP32 seine CPU- beziehungsweise Taktfrequenz direkt nach dem Start so weit wie praktikabel reduzieren, damit die Leistungsaufnahme auf der Tag-Seite kleiner wird.

Die TX- und RX-LEDs sollen nicht durch zusätzliche Blinkprogramme simuliert werden. Falls ein sichtbares Aufleuchten nicht direkt durch die Leitung möglich ist, wird entweder eine einfache externe Beschaltung oder eine minimale Flankenerkennung im ESP32 verwendet.

Der vollständige Programmcode für die beiden ESP32 wird nicht direkt in diese Dokumentation eingefügt, sondern im zugehörigen GitHub-Repository abgelegt. Dadurch bleibt die Dokumentation übersichtlich und der Code kann unabhängig davon weiterbearbeitet werden. Das Repository ist unter folgender Adresse erreichbar:
\begin{center}
  \url{https://github.com/M08o/HWE-Prot-Doku.git}
\end{center}

\subsection{Programmablauf als Flussdiagramm}
Die folgenden Flussdiagramme fassen den Ablauf der beiden Programme zusammen. Sie beziehen sich auf die im Repository abgelegten Dateien für Lesegerät und Tag.
Der Programmablauf des Lesegeräts ist in \cref{fig:flussdiagramm_reader} dargestellt. Der Ablauf des Tags ist in \cref{fig:flussdiagramm_tag} dargestellt.

\tikzset{
  flowstart/.style={
    rectangle,
    rounded corners=3pt,
    draw,
    fill=green!18,
    align=center,
    minimum width=3.3cm,
    minimum height=0.8cm
  },
  flowstep/.style={
    rectangle,
    draw,
    fill=blue!10,
    align=center,
    text width=4.1cm,
    minimum height=0.9cm
  },
  flowdecision/.style={
    diamond,
    draw,
    fill=orange!18,
    aspect=2.1,
    align=center,
    text width=3.0cm,
    inner sep=1pt
  },
  flowarrow/.style={-Latex, thick, draw=blue!60!black}
}

\begin{figure}[H]
  \centering
  \resizebox{!}{0.72\textheight}{%
  \begin{tikzpicture}[node distance=1.15cm and 1.7cm]
    \node[flowstart] (start) {Start Lesegerät};
    \node[flowstep, below=of start] (init) {Serial Monitor starten, UART2 und LEDs initialisieren};
    \node[flowstep, below=of init] (waitstart) {Auf Startbyte \texttt{0xAA} vom Tag warten};
    \node[flowdecision, below=of waitstart] (startok) {Startbyte korrekt?};
    \node[flowstep, right=of startok] (ignore) {Byte verwerfen und weiter warten};
    \node[flowstep, below=of startok] (ack) {ACK \texttt{0xAC} senden und aktiven Modus starten};
    \node[flowstep, below=of ack] (waitbyte) {Auf Byte vom Tag warten};
    \node[flowdecision, below=of waitbyte] (timeout) {Timeout?};
    \node[flowstep, right=of timeout] (reset) {Zurück in den Startzustand};
    \node[flowdecision, below=of timeout] (toggle) {Byte = \texttt{0xF0}?};
    \node[flowstep, right=of toggle] (leds) {LED-Toggle-Modus umschalten};
    \node[flowstep, below=of toggle] (delay) {$3\,s$ Antwortpause abwarten};
    \node[flowstep, below=of delay] (reply) {Antwortbyte aus der \texttt{0xB0}-Reihe senden};

    \draw[flowarrow] (start) -- (init);
    \draw[flowarrow] (init) -- (waitstart);
    \draw[flowarrow] (waitstart) -- (startok);
    \draw[flowarrow] (startok) -- node[above] {nein} (ignore);
    \draw[flowarrow] (ignore) |- (waitstart);
    \draw[flowarrow] (startok) -- node[right] {ja} (ack);
    \draw[flowarrow] (ack) -- (waitbyte);
    \draw[flowarrow] (waitbyte) -- (timeout);
    \draw[flowarrow] (timeout) -- node[above] {ja} (reset);
    \draw[flowarrow] (reset.east) -- ++(1.0,0) |- (waitstart.east);
    \draw[flowarrow] (timeout) -- node[right] {nein} (toggle);
    \draw[flowarrow] (toggle) -- node[above] {ja} (leds);
    \draw[flowarrow] (leds) |- (delay);
    \draw[flowarrow] (toggle) -- node[right] {nein} (delay);
    \draw[flowarrow] (delay) -- (reply);
    \draw[flowarrow] (reply.west) -- ++(-1.5,0) |- (waitbyte.west);
  \end{tikzpicture}
  }
  \caption{Flussdiagramm des ESP32-Lesegeräts}
  \label{fig:flussdiagramm_reader}
\end{figure}

\begin{figure}[H]
  \centering
  \resizebox{!}{0.72\textheight}{%
  \begin{tikzpicture}[node distance=1.15cm and 1.7cm]
    \node[flowstart] (start) {Start Tag};
    \node[flowstep, below=of start] (init) {CPU-Frequenz reduzieren, LEDs und UART2 initialisieren};
    \node[flowstep, below=of init] (sendstart) {Startbyte \texttt{0xAA} senden};
    \node[flowstep, below=of sendstart] (flush) {Echo der eigenen Sendung kurz verwerfen};
    \node[flowstep, below=of flush] (waitack) {Auf ACK \texttt{0xAC} vom Lesegerät warten};
    \node[flowdecision, below=of waitack] (ackok) {ACK korrekt?};
    \node[flowstep, right=of ackok] (restart) {Bei Timeout Handshake neu starten};
    \node[flowstep, below=of ackok] (payload) {Nutzbyte senden; jedes fünfte Byte ist \texttt{0xF0}};
    \node[flowstep, below=of payload] (waitreader) {Auf Antwortbyte vom Lesegerät warten};
    \node[flowdecision, below=of waitreader] (received) {Antwort empfangen?};
    \node[flowstep, below=of received] (delay) {$3\,s$ warten};
    \node[flowstep, below=of delay] (next) {Nächstes Nutzbyte oder Toggle-Befehl senden};

    \draw[flowarrow] (start) -- (init);
    \draw[flowarrow] (init) -- (sendstart);
    \draw[flowarrow] (sendstart) -- (flush);
    \draw[flowarrow] (flush) -- (waitack);
    \draw[flowarrow] (waitack) -- (ackok);
    \draw[flowarrow] (ackok) -- node[above] {nein} (restart);
    \draw[flowarrow] (restart) |- (sendstart);
    \draw[flowarrow] (ackok) -- node[right] {ja} (payload);
    \draw[flowarrow] (payload) -- (waitreader);
    \draw[flowarrow] (waitreader) -- (received);
    \draw[flowarrow] (received.east) -- ++(1.4,0) |- node[pos=0.25, right] {nein} (waitreader.east);
    \draw[flowarrow] (received) -- node[right] {ja} (delay);
    \draw[flowarrow] (delay) -- (next);
    \draw[flowarrow] (next.west) -- ++(-1.5,0) |- (waitreader.west);
  \end{tikzpicture}
  }
  \caption{Flussdiagramm des ESP32-Tags}
  \label{fig:flussdiagramm_tag}
\end{figure}
